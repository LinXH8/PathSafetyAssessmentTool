# --- Core data / geo ---
pandas>=2.0,<3   # pandas 3 changed the default string dtype: treatments.py apply_treatments (int into str column) and possibly more break under it
numpy>=1.24
geopandas>=0.13.2
shapely>=2.0
pyproj>=3.5
fiona>=1.9
rtree>=1.2    # Optional but strongly recommended: speeds up sindex spatial indexing

# --- Viz ---
matplotlib>=3.7

# --- Media / imaging ---
opencv-python>=4.8
Pillow>=10.0

# --- Geospatial utils ---
geopy>=2.3

# --- Excel / Windows COM ---
openpyxl>=3.1
pywin32>=306; sys_platform == 'win32'   # Provides pythoncom / win32com (Windows only)

# --- Networking ---
requests>=2.31

# --- Optional: UI / caching in gis_mapping ---
streamlit>=1.32

# --- Optional: CV auto-coding (prediction.py) ---
ultralytics>=8.0

# If ultralytics fails to auto-install torch, uncomment the next line and select the right version/platform wheel:
# torch==2.2.*  # CPU: official pip wheel; GPU: use the torch wheel matching your CUDA/cuDNN version

Flask>=3.0
flask-cors>=4.0
waitress>=3.0   # Production WSGI server (app.py). Dev server only via PSAT_DEV=1.
exifread>=3.0    # Required: backend routes.py imports exifread for EXIF metadata extraction
python-pptx>=0.6
python-docx>=1.1

# CPU torch + ultralytics are installed by Run-PSAT.bat before requirements.txt,
# so the lines above ensure they are present if someone installs manually.
